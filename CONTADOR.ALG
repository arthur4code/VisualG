algoritmo "contador"

var
   contador:inteiro
   
   
inicio
      contador <- 0
      enquanto (contador<=3) faca
               escreval(contador)
                       limpatela
               contador <- contador +1

      fimenquanto

fimalgoritmo
